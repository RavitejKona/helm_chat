---
apiVersion: v1
kind: Service
metadata:
   name: {{.Values.embed.service.service_name}}
   namespace: {{.Values.namespace}}
   labels:
      app: {{.Values.embed.app_name}}
spec:
   selector:
      app: {{.Values.embed.app_name}}
   type: NodePort
   ports:
     - name: {{.Values.embed.service.port_name}}
       protocol: TCP
       port: {{.Values.embed.service.port}}
       targetPort: {{.Values.embed.service.port}}